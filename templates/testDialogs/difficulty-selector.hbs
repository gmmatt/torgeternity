<fieldset>
  <legend>{{localize "torgeternity.sheetLabels.dn"}}</legend>
  <span class="skill-check-options">
    <select id="difficulty" name="DNDescriptor" class="input-box" data-dtype="String">
      {{#if test.targetPresent}} {{selectOptions config.dnTypes selected=test.DNDescriptor
      localize=true}} {{else}} {{selectOptions config.dnTypesBasic selected=test.DNDescriptor
      localize=true}} {{/if}}
    </select>
  </span>
</fieldset>